{
  "name": "mordomo-top-prod",
  "type": "module",
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
