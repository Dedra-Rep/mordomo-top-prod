{
  "name": "mordomo-top-prod",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node server/index.js",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix web ci && npm --prefix web run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "@google/genai": "^1.7.0",
    "express": "^4.19.2"
  }
}
